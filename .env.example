# Telegram Bot Token (use different bot token for each environment)
TELEGRAM_BOT_TOKEN=123456789:your_bot_token_here

# Telegram ID of your account
TELEGRAM_ADMIN_ID=987654321

# Environment (auto-detected)
ENVIRONMENT=local

# Frontend URL for CORS validation (optional)
# Restricts which origins can call the backend API
# If not set, defaults to wildcard (*) - not recommended for production
#
# For local development:
# - Browser testing: http://localhost:3000
# - Telegram bot testing: Use ngrok tunnel URL (get with `npm run tunnel:status`)
#
# Examples:
# PAGES_URL=http://localhost:3000
# PAGES_URL=https://your-tunnel-url.ngrok-free.app
# PAGES_URL=https://your-app.pages.dev

# Bypass Telegram authentication for local dev & tests
# Set to "true" to enable mock auth (allows browser testing without Telegram)
# The user role will be determined by comparing the mock user ID with TELEGRAM_ADMIN_ID.
DEV_AUTH_BYPASS_ENABLED=false
